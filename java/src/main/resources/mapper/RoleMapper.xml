<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.highnes.attractinvestment.mapper.RoleMapper">
    <resultMap id="BaseResultMap" type="com.highnes.attractinvestment.entity.Role">
        <id column="role_id" property="roleId" />
        <result column="role_name" property="roleName" />
        <result column="role_key" property="roleKey" />
        <result column="role_sort" property="roleSort" />
        <result column="data_scope" property="dataScope" />
        <result column="status" property="status" />
        <result column="del_flag" property="delFlag" />
        <result column="create_by" property="createBy" />
        <result column="create_date" property="createDate" />
        <result column="update_by" property="updateBy" />
        <result column="update_date" property="updateDate" />
        <result column="remark" property="remark" />
    </resultMap>
    <resultMap extends="BaseResultMap" id="PermissionMap" type="com.highnes.attractinvestment.entity.Role">
        <collection  property="permissions" ofType="com.highnes.attractinvestment.entity.Permission">
            <id column="id" property="id"/>
            <result column="pid" property="pid"/>
            <result column="name" property="name"/>
            <result column="value" property="value"/>
            <result column="icon" property="icon"/>
            <result column="type" property="type"/>
            <result column="uri" property="uri"/>
            <result column="status" property="status"/>
            <result column="create_time" property="createTime"/>
            <result column="sort" property="sort"/>
        </collection >
    </resultMap>


    <select id="findRolePermissionByUserId" resultMap="PermissionMap">
        select
        *
        from sys_user_role sur
        left join sys_role sr on sur.role_id = sr.role_id
        left join sys_role_permission_relation srp on srp.role_id = sr.role_id
        left join sys_permission sp on srp.permission_id = sp.id
        where sur.user_id = #{userId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from sys_role
    </select>

    <update id="update" parameterType="com.highnes.attractinvestment.entity.Role">
        update sys_role
        <trim prefix="SET" suffixOverrides=",">
            <if test="roleName != null and roleName != ''">role_name=#{roleName},</if>
            <if test="roleKey != null and roleKey != ''">role_key=#{roleKey},</if>
            <if test="roleSort != null and roleSort != ''">role_sort=#{roleSort},</if>
            <if test="dataScope != null and dataScope != ''">data_scope=#{dataScope},</if>
            <if test="status != null and status != ''">status=#{status},</if>
            <if test="delFlag != null and delFlag != ''">del_flag=#{delFlag},</if>
            <if test="createBy != null and createBy != ''">create_by=#{createBy},</if>
            <if test="createDate != null and createDate != ''">create_date=#{createDate},</if>
            <if test="updateBy != null and updateBy != ''">update_by=#{updateBy},</if>
            <if test="updateDate != null and updateDate != ''">update_date=#{updateDate},</if>
            <if test="remark != null and remark != ''">remark=#{remark},</if>
        </trim>
        where role_id = #{roleId}
    </update>

    <insert id="insert" parameterType="com.highnes.attractinvestment.entity.Role">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into sys_role (
        role_name,
        role_key,
        role_sort,
        data_scope,
        status,
        del_flag,
        create_by,
        create_date,
        update_by,
        update_date,
        remark
        ) value
        (
        #{roleName},
        #{roleKey},
        #{roleSort},
        #{dataScope},
        #{status},
        #{delFlag},
        #{createBy},
        #{createDate},
        #{updateBy},
        #{updateDate},
        #{remark}
        )
    </insert>

    <delete id="deleteById" parameterType="com.highnes.attractinvestment.entity.Role">
        delete * from sys_role where role_id = #{roleId}
    </delete>


</mapper>