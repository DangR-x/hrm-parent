<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.highnes.attractinvestment.mapper.RolePermissionMapper">
    <resultMap id="BaseResultMap" type="com.highnes.attractinvestment.entity.RolePermission">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="role_id" jdbcType="BIGINT" property="roleId" />
        <result column="permission_id" jdbcType="BIGINT" property="permissionId" />

    </resultMap>

    <select id="selectByQuery" resultMap="BaseResultMap">
        select * from
        sys_role_permission_relation a
        <where>
            <if test="id != null and id != ''">and a.id = #{id}</if>
            <if test="roleId != null and roleId != ''">and a.role_id = #{roleId}</if>
            <if test="permissionId != null and permissionId != ''">and a.permission_id = #{permissionId}</if>
        </where>
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from sys_role_permission_relation
    </select>

    <update id="update" parameterType="com.highnes.attractinvestment.entity.RolePermission">
        update sys_role_permission_relation set
            role_id=#{roleId},
            permission_id=#{permissionId}
        where id = #{id}
    </update>

    <insert id="insert" parameterType="com.highnes.attractinvestment.entity.RolePermission">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into sys_role_permission_relation (
        role_id,
        permission_id
        ) value
        (
        #{roleId},
        #{permissionId}
        )
    </insert>

    <delete id="deleteById" parameterType="com.highnes.attractinvestment.entity.RolePermission">
        delete * from sys_role_permission_relation where id = #{id}
    </delete>


</mapper>