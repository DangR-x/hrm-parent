<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.highnes.attractinvestment.mapper.SysUserRoleMapper">
    
    <sql id="sysUserRoleColumns">
    a.user_id AS "userId",
        a.role_id AS "roleId"        </sql>

    <sql id="sysUserRoleLeftJoin">
    </sql>

    <select id="get" resultType="SysUserRole">
        SELECT
        <include refid="sysUserRoleColumns"/>
        FROM
        sys_user_role a
        <include refid="sysUserRoleLeftJoin"/>
        WHERE a.id = #{id}
    </select>

    <select id="findList" resultType="SysUserRole">
        SELECT
        <include refid="sysUserRoleColumns"/>
        FROM
        sys_user_role a
        <include refid="sysUserRoleLeftJoin"/>
        <where>
            1=1
        </where>
    </select>

    <select id="selUserRoleListByUserId" resultType="SysUserRole">
        SELECT
        <include refid="sysUserRoleColumns"/>
        FROM
        sys_user_role a
        <include refid="sysUserRoleLeftJoin"/>
        <where>
            1=1
            <if test="userId != null and userId != ''">and user_id = #{userId}</if>
        </where>
    </select>


    <insert id="insert" parameterType="SysUserRole">
        insert into sys_user_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null ">user_id,</if>
            <if test="roleId != null ">role_id,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null ">#{userId},</if>
            <if test="roleId != null ">#{roleId},</if>
         </trim>
    </insert>

    <update id="update" parameterType="SysUserRole">
        update sys_user_role
        <trim prefix="SET" suffixOverrides=",">
            <if test="roleId != null ">role_id = #{roleId},</if>
        </trim>
        where user_id = #{userId}
    </update>

    <delete id="delete" parameterType="java.lang.String">
        delete from sys_user_role where user_id = #{userId}
    </delete>

</mapper>